language: c
script: dpkg-buildpackage -b -us -uc

install:
   - sudo apt-get install omake noweb valgrind dh-ocaml libbatteries-ocaml-dev libsexplib-camlp4-dev libderiving-ocsigen-ocaml-dev liblwt-ocaml-dev

# whitelist
branches:
   only:
      - master
      - /.*\/staging/
